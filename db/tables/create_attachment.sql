CREATE COLUMN TABLE "MKTG_CART_REQUEST_TOOL".ATTACHMENT
(
	ATTACHMENT_ID BIGINT NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	USER_ID BIGINT NOT NULL,
	ORIGINAL_NAME NVARCHAR(255) NOT NULL,
	SAVED_NAME NVARCHAR(255) NOT NULL,
	ATTACHMENT_SIZE BIGINT NOT NULL,
	ATTACHMENT_TYPE NVARCHAR(255) NOT NULL,

	CREATED_USER_ID BIGINT NOT NULL,
	MODIFIED_USER_ID BIGINT,
	CREATED_DATE_TZ TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	MODIFIED_DATE_TZ TIMESTAMP,
	ENABLED TINYINT DEFAULT 1,
	DELETED TINYINT DEFAULT 0,
)
ALTER TABLE "MKTG_CART_REQUEST_TOOL"."ATTACHMENT" ADD FOREIGN KEY ( "USER_ID" ) REFERENCES "MKTG_CART_REQUEST_TOOL"."USER" ("USER_ID") ON UPDATE RESTRICT ON DELETE RESTRICT
;