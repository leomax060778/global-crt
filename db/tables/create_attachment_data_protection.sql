CREATE COLUMN TABLE "MKTG_CART_REQUEST_TOOL".ATTACHMENT_DATA_PROTECTION
(
	ATTACHMENT_DATA_PROTECTION_ID BIGINT NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
	DESCRIPTION NVARCHAR(255) NOT NULL,
	ATTACHMENT_ID BIGINT NOT NULL,

	CREATED_DATE_TZ TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
	MODIFIED_DATE_TZ TIMESTAMP,
	CREATED_USER_ID BIGINT NOT NULL,
	MODIFIED_USER_ID BIGINT,
	ENABLED TINYINT DEFAULT 1,
	DELETED TINYINT DEFAULT 0
);

ALTER TABLE "MKTG_CART_REQUEST_TOOL"."ATTACHMENT_DATA_PROTECTION" ADD FOREIGN KEY ( "ATTACHMENT_ID" ) REFERENCES "MKTG_CART_REQUEST_TOOL"."ATTACHMENT" ("ATTACHMENT_ID") ON UPDATE RESTRICT ON DELETE RESTRICT
;